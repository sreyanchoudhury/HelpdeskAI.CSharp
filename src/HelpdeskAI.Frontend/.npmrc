# Only fail builds on high/critical vulnerabilities.
# The moderate-severity advisories (langsmith SSRF, prismjs DOM Clobbering) are
# deep transitive deps inside CopilotKit that cannot be fixed without a breaking
# downgrade. They will resolve when CopilotKit ships an upstream patch.
audit-level=high
